<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.rfnbrgr.kitchenthermometer">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name="de.rfnbrgr.kitchenthermometer.HeatDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="de.rfnbrgr.kitchenthermometer.SettingsActivity" />
        <activity
            android:name=".AlarmSettingsActivity"
            android:theme="@style/AppTheme.SettingsTheme" />

        <service android:name="de.rfnbrgr.kitchenthermometer.DeviceClientService" />
    </application>

</manifest>